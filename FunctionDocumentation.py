#__doc__ is a special attribute that contains the documentation string of the function. In this case, it will print the documentation for the built-in abs() function, which returns the absolute value of a number.

print(abs.__doc__)